# Installing Guide

Follow each section:

1. [Download the project](download.md)
2. [Install the prerequisite](prerequisite.md)
3. [Install the tools](tools.md)

## Verifying

To verify if everything is installed and set up correctly,
try to build the game by running the following commands inside the project folder:
`make -s resource` and then `make -s` (or on Windows run the `run_make.bat` file and choose `All`).
If you don't get any errors, everything is fine!

### Troubleshooting

*TODO: Common issues*

If nothing has resolved your problem...
try to google it,
or create an issue on the GitHub (if no other issues talk about your problem).
I (or someone else) may see it one day and answer it (but no promise).
